<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= guide.title %> | Snyk Security Guide</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/themes/prism.min.css" rel="stylesheet" />
</head>
<body class="bg-gray-100 min-h-screen">
  <header class="bg-purple-900 text-white p-6 shadow-md">
    <div class="container mx-auto flex justify-between items-center">
      <div>
        <h1 class="text-3xl font-bold">Snyk Security Guide</h1>
        <p class="mt-2">Best practices for using Snyk with different languages and frameworks</p>
      </div>
      <a href="/" class="bg-white text-purple-900 px-4 py-2 rounded hover:bg-gray-100 transition">Back to Home</a>
    </div>
  </header>

  <main class="container mx-auto py-8 px-4">
    <% if (guide.error) { %>
      <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 mb-8" role="alert">
        <p><%= guide.error %></p>
        <a href="/" class="text-blue-600 underline mt-2 inline-block">Return to selection</a>
      </div>
    <% } else { %>
      <div class="bg-white rounded-lg shadow-md p-6 mb-8">
        <h1 class="text-3xl font-bold mb-4"><%= guide.title %></h1>
        <p class="text-lg mb-6"><%= guide.description %></p>
        
        <div class="mb-8">
          <h2 class="text-2xl font-bold mb-4">Installation</h2>
          <div class="bg-gray-100 p-4 rounded-lg">
            <pre class="language-bash"><code><%- guide.installation.replace(/```\n|```/g, '') %></code></pre>
          </div>
        </div>

        <div class="mb-8">
          <h2 class="text-2xl font-bold mb-4">Basic Usage</h2>
          <div class="bg-gray-100 p-4 rounded-lg">
            <pre class="language-bash"><code><%- guide.usage.replace(/```\n|```/g, '') %></code></pre>
          </div>
        </div>

        <div class="mb-8">
          <h2 class="text-2xl font-bold mb-4">SCM Integration</h2>
          <p class="mb-4"><%= guide.scmIntegration %></p>
        </div>

        <div class="mb-8">
          <h2 class="text-2xl font-bold mb-4">CLI Commands</h2>
          <div class="bg-white border border-gray-200 rounded-lg overflow-hidden">
            <div class="p-4 border-b border-gray-200 bg-gray-50">
              <h3 class="font-bold"><code><%= guide.cliBasics.command %></code></h3>
              <p class="mt-1 text-gray-600"><%= guide.cliBasics.description %></p>
            </div>
            <div class="p-4">
              <h4 class="font-bold mb-2">Parameters:</h4>
              <ul class="list-disc pl-5 space-y-1">
                <% guide.cliBasics.parameters.forEach(param => { %>
                  <li><code><%= param.split(':')[0] %></code>: <%= param.split(':')[1] %></li>
                <% }); %>
              </ul>
            </div>
          </div>
        </div>

        <div class="mb-8">
          <h2 class="text-2xl font-bold mb-4">Best Practices</h2>
          <ul class="list-disc pl-6 space-y-2">
            <% guide.bestPractices.forEach(practice => { %>
              <li><%= practice %></li>
            <% }); %>
          </ul>
        </div>

        <div>
          <h2 class="text-2xl font-bold mb-4">Common Security Issues</h2>
          <ul class="list-disc pl-6 space-y-2">
            <% guide.commonIssues.forEach(issue => { %>
              <li><%= issue %></li>
            <% }); %>
          </ul>
        </div>
      </div>
    <% } %>
  </main>

  <footer class="bg-gray-800 text-white p-6 mt-8">
    <div class="container mx-auto">
      <p>&copy; 2025 Snyk Security Guide - An unofficial guide to Snyk best practices</p>
    </div>
  </footer>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/components/prism-core.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/prism/1.24.1/plugins/autoloader/prism-autoloader.min.js"></script>
</body>
</html>